stages:
  - deploy

pages:
  image: alpine:3.20
  stage: deploy
  script:
    - mkdir public
    - cp -r css js *.html config.json public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
